# DAOS v4 Environment Variables / 环境变量 (copy to .env / 复制到 .env)
PG_PASSWORD=change_me_now       # PostgreSQL password / PostgreSQL 密码
GRAFANA_PASSWORD=daos_admin      # Grafana admin password / Grafana 管理员密码
PROMETHEUS_URL=http://prometheus:9090  # Prometheus URL / Prometheus 地址
